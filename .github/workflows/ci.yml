name: CI

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: |-
    ${{ github.ref != format('refs/heads/{0}', github.event.repository.default_branch) }}

on:
  push:
    branches:
      - main
      - epic/*
      - "[0-9]+.[0-9]+.x"
  pull_request:
    branches:
      - main
      - epic/*
      - "[0-9]+.[0-9]+.x"
  workflow_dispatch:

jobs:
  doc:
    uses: ComPWA/actions/.github/workflows/ci-docs.yml@24413cd08a4657e18a344713ff7e91360dff9b0d # v4.1.0
    permissions:
      pages: write
      id-token: write
    with:
      gh-pages: true
  style:
    secrets:
      token: ${{ secrets.PAT }}
    uses: ComPWA/actions/.github/workflows/style.yml@24413cd08a4657e18a344713ff7e91360dff9b0d # v4.1.0
  test:
    uses: ComPWA/actions/.github/workflows/test.yml@24413cd08a4657e18a344713ff7e91360dff9b0d # v4.1.0
    secrets:
      CODECOV_TOKEN: ${{ secrets.CODECOV_TOKEN }}
    with:
      coverage-python-version: "3.13"
      macos-python-version: "3.10"
      multithreaded: false
